<p id="notice"><%= notice %></p>

<h1>Listing Tracks</h1>

<table>
  <thead>
    <tr>
      <th>Itunes</th>
      <th>Title</th>
      <th>Artist</th>
      <th>Number</th>
      <th>Genre</th>
      <th>Album</th>
      <th>Cover url</th>
      <th>Preview url</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tracks.each do |track| %>
      <tr>
        <td><%= track.iTunes_id %></td>
        <td><%= track.title %></td>
        <td><%= track.artist %></td>
        <td><%= track.number %></td>
        <td><%= track.genre %></td>
        <td><%= track.album %></td>
        <td><%= track.cover_url %></td>
        <td><%= track.preview_url %></td>
        <td><%= link_to 'Show', track %></td>
        <td><%= link_to 'Edit', edit_track_path(track) %></td>
        <td><%= link_to 'Destroy', track, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Track', new_track_path %>
